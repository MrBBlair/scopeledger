# Firebase
VITE_FIREBASE_API_KEY=
VITE_FIREBASE_AUTH_DOMAIN=
VITE_FIREBASE_PROJECT_ID=
VITE_FIREBASE_STORAGE_BUCKET=
VITE_FIREBASE_MESSAGING_SENDER_ID=
VITE_FIREBASE_APP_ID=

# Admin (comma-separated Firebase UIDs)
VITE_ADMIN_UIDS=

# Optional: API base for email (defaults to same origin)
# VITE_API_BASE=

# Email (Postmark) – see docs/EMAIL_SETUP.md
# For local dev: add to .env.local and run `vercel dev` (not `npm run dev`) for /api/email
# POSTMARK_API_KEY=
# EMAIL_FROM=noreply@yourdomain.com

# Server-only: Postmark bounce webhook (optional)
# POSTMARK_WEBHOOK_SECRET=

# Server-only: Unsubscribe link token signing (required for unsubscribe links in emails)
# UNSUBSCRIBE_SECRET=

# Server-only: Firebase Admin for email suppression check and webhook (optional)
# FIREBASE_SERVICE_ACCOUNT=
